'use strict'

module.exports = function(io, socket) {

    // Temporary. We suppose here that after been connected
    // The user, send his username to say through a Welcome message
    // And it get an answer with another Welcome message
  // socket.on('Welcome', function(username) {
  //     console.log("Welcome ... " + username);
  //     io.emit('Welcome', 'Welcome ' + username);
  // });
}
